$include "sdk/stdlib.evm"
array8u buf[128];
function main() {
	local f, n;
	if ( argc == 1 ) {
		argv[1] = string("");
	}
	f = file_list(argv[1]);
	while(!eof(f)) {
		fgets(f, &buf);
		fputs(stdout, &buf);
		fputc(stdout, 10);
	}
	close(f);
}